# 海风首版验收 · 2026-09-23

## 结论
本机软件首版已实现，真实视觉模型和USB音频输出链路已跑通；完整硬件交付尚未通过。眼镜未被现场扫描发现；Reachy Daemon 报告没有检测到电机，需要检查独立供电。音频已送达指定Reachy输出设备，现场可闻性仍未人工确认。

## 实测证据
| 项目 | 结果 | 证据与边界 |
|---|---|---|
| 私有Git恢复 | 通过 | origin为kimniniup-creator/haifeng，保留上游提交历史 |
| 视觉模型 | 通过 | 实际调用DeepSeek deepseek-flash，测试图红方块/蓝圆正确识别；凭据仅本机 |
| 页面完整流程 | 通过 | 上传测试PNG→真实模型答案→系统音频路由→刷新恢复照片和两条对话 |
| 本机持久化 | 通过 | 刷新后原图400px宽、对话两条恢复；命名和删除通过 |
| 取消眼镜拍照 | 通过软件路径 | 实际请求后停止，后台子进程取消，页面退出等待；未获得真机照片 |
| 模型失败UI | 通过 | 浏览器拦截注入失败响应，文字保留、不制造答案；此项是故障注入 |
| USB音频 | 软件播放通过 | Windows SAPI中文WAV，重采样至44100Hz并送到Reachy MME输出；不是电脑默认扬声器；audio_status=played_unverified |
| Reachy运动 | 阻塞 | SDK 1.11.0 / COM11 / sim=false，No motors detected；不将HTTP200当已连接 |
| Luma真机 | 阻塞 | 本机BLE扫描未发现E06/E09或AA12；Python后备实现仅协议测试通过 |
| 桌面和手机布局 | 通过 | Edge无头真实服务1400×900与390×844，无横向溢出、无页面脚本异常 |
| 连续20轮 | 模拟通过 | tests/test_twenty_rounds.py；20组不同颜色图、40次幂等提交仅执行20轮、顺序/上下文/图片对应正确；不是硬件稳定性结论 |

## 自动化测试
`D:\海风\.venv\Scripts\python.exe -m pytest -q`：19项通过。覆盖队列、取消、音频失败、重启中断、归属、配置持久化、回环Origin检查、BLE帧校验与重组、机器人状态和输出设备选择、20轮模拟。

`python -m compileall -q bridge`、`node --check bridge/static/app.js`通过。

## 页面检查
桌面和手机截图位于本机 `qa-artifacts/`，按临时验收产物处理，不上传Git。
- `desktop-empty.png` / `mobile-empty.png`：清理测试记录后的页面。
- `desktop-final.png` / `mobile-final.png`：测试图和真实模型答案；最后一次截图对长状态文案做了前端排版压力注入，不能据此证明新的硬件播放。
- 初次完整浏览器脚本末尾“列表必须为0”断言失败，原因是首次等待时间不足而跳过了上一轮测试记录；复检按实际API完成事件等待，命名/删除/恢复通过。未把这次脚本失败归为产品成功。

## 尚未通过
1. Reachy独立供电后重试Daemon，确认真实小幅触角动作与完成事件。
2. 眼镜开机唤醒、退出占用蓝牙的手机应用，重试拍照；Python协议实现需实际JPEG验证。
3. 人耳确认声音来自Reachy，记录可闻性；当前不把软件播放结束当作可闻验收。
4. 全部设备就绪后的20轮真实拍照/问答/动作/声音稳定性、手机实体局域网访问。

## 数据处理
本轮只向模型发送人工生成的红方块/蓝圆测试图和测试文字，没有拍摄房间或私人画面。浏览器验证产生的片段均通过应用删除。`.env`、`data/`、`.runtime/`、截图和依赖环境不进入Git。
